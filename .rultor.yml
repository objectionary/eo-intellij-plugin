assets:
  tokenscript.txt: yegor256/objectionary-secrets#marketplace.token.dirty.sh


release:
  script: |-
    source ../tokenscript.txt
    echo "TOKEN IS PRINTED BELOW"
    echo $MARKETPLACE_TOKEN
    ./gradlew -PpublishToken=$MARKETPLACE_TOKEN publishPlugin --console=plain | perl -pe 's/\x1b\[[0-9;]*[mG]//g'